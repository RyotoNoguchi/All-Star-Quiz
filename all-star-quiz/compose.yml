name: all-star-quiz
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: quiz
      POSTGRES_PASSWORD: quiz
      POSTGRES_DB: quiz
    ports:
      - '127.0.0.1:55432:5432'
    volumes:
      - quiz-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U quiz -d quiz']
      interval: 2s
      timeout: 5s
      retries: 20
volumes:
  quiz-postgres:
